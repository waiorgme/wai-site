---
import Eyebrow from '../primitives/Eyebrow.astro';

// The three tiers (S4): Supporter / Partner / Champion. Benefit copy is verbatim.
// LOCK 6: no AED figures anywhere (the tier amounts are internal only and never
// rendered). LOCK 2 (gold = recognition only): the wow-elevation pass (C3) ruled
// Champion a genuine recognition moment (it funds the pilot licence), so Champion
// alone carries the gold treatment; Supporter stays outline and Partner fills navy
// so the three escalate visually. Labels stay JetBrains Mono.
// The closing line under the tiers is verbatim.
const tiers = [
  {
    name: 'Supporter',
    tone: 'tier-supporter',
    body: 'A company beginning its commitment to women in aviation: the partner seal for the year, named recognition, and a share in the yearly impact report.',
  },
  {
    name: 'Partner',
    tone: 'tier-partner',
    body: 'A company backing real outcomes: everything in Supporter, plus a larger role at events and in the talent pipeline.',
  },
  {
    name: 'Champion',
    tone: 'tier-champion',
    body: 'A company funding life-changing access: everything in Partner, plus headline recognition and enough to fund a full pilot licence or several scholarships at our partner academies.',
  },
];
---
<section class="inv-tiers" id="tiers" aria-labelledby="tiers-h">
  <div class="wrap">
    <div class="head reveal">
      <Eyebrow onPaper>The three tiers</Eyebrow>
      <h2 id="tiers-h">The three tiers</h2>
    </div>

    <ol class="tier-grid">
      {tiers.map((t, i) => (
        <li class={`tier-card ${t.tone} reveal`} style={`--i:${i}`}>
          <span class="tier-label" aria-hidden="true">{String(i + 1).padStart(2, '0')} / {t.name}</span>
          <h3 class="tier-name">{t.name}</h3>
          <p>{t.body}</p>
        </li>
      ))}
    </ol>

    <p class="tier-closing reveal" style="--i:3">Every tier carries an internship commitment, so partners help build the pipeline, not just fund it.</p>
  </div>
</section>
