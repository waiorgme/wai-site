// Admin console class names (panel-design slice): the same light panel system
// as the portal (src/styles/panel.css), plus the console-only layout bits.
// Still no gold anywhere in admin - gold is recognition-only and nothing here
// is a recognition moment. These are className strings, used as
// className={...} (never style={...}).

export const queueSection = "pn-card pn-queue pn-stack";
export const rowCard = "pn-row";
export const rowMeta = "pn-meta";
export const rowName = "pn-name";
export const tag = "pn-tag";
