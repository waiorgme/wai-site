# build output
dist/
.astro/

# test artifacts
test-results/
playwright-report/
playwright/.cache/

# deps
node_modules/

# os / editor
.DS_Store
*.log

# env
.env
.env.*
!.env.example

# convex: _generated IS committed so the static build (and CI) resolves the
# api/types without a Convex deployment. Regenerated by `npx convex dev`.
