// The ONE site origin. Canonical URLs, og:url, hreflang, the sitemap and the
// Astro `site` config all import this, so they can never disagree. Matches
// the live domain's canonical www form.
export const SITE = 'https://www.waiorg.me';
